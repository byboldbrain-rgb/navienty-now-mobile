name: Apply Private Upload Validation

on:
  push:
    branches:
      - cto/launch-readiness-hardening
    paths:
      - .github/workflows/apply-private-upload-validation.yml
      - scripts/apply-private-upload-validation.mjs
      - src/domain/private-upload-validation.ts
      - tests/private-upload-validation.test.mjs

permissions:
  contents: write

env:
  CI: '1'

jobs:
  harden:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout hardening branch
        uses: actions/checkout@v5
        with:
          ref: cto/launch-readiness-hardening
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply exact source patch
        run: node scripts/apply-private-upload-validation.mjs

      - name: Business/security tests
        run: npm test

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Production dependency audit policy
        run: npm run audit:prod

      - name: Show source diff
        run: git diff -- src/services/order-payment-proof-service.ts src/services/prescription-service.ts

      - name: Commit verified private upload hardening
        run: |
          if git diff --quiet -- src/services/order-payment-proof-service.ts src/services/prescription-service.ts; then
            echo "No upload-service changes to commit."
            exit 0
          fi
          git config user.name "Navienty CTO Automation"
          git config user.email "actions@users.noreply.github.com"
          git add src/services/order-payment-proof-service.ts src/services/prescription-service.ts
          git commit -m "security: validate private upload file signatures"
          git push origin HEAD:cto/launch-readiness-hardening
